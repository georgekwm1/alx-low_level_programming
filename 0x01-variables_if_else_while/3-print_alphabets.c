#include <stdio.h>

int main()
{
char ch


        for (ch = 'a'; ch <= 'z';)
	  {
        putchar("%c \n", ch);
        ch++;
	  }

        for (ch = 'A'; ch <= 'Z';)
	  {
        putchar("%c \n", ch);
        ch++;
	  }

}
