#include "main.h"

/* more headers goes there */

/* betty style doc for function main goes there */
/**
 * main - print putchar
 * Return: always 0
 */
int _isdigit(int c)
{
for (c = 0; c <= 9; c++)
{
if (c <= 9)
{
return (1);
}
}
return (0);
}
